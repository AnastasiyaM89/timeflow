import styled from 'styled-components';

const LoaderWrapper = styled.div`
	text-align: center;
	padding: 20px;
	font-size: 18px;
`;

export const Loader = () => <LoaderWrapper>Загрузка...</LoaderWrapper>;
